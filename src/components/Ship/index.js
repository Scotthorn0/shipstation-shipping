import React, { Component } from 'react';
import { Menu, Container, Header } from 'semantic-ui-react';
import './styles.css';

const Ship = () => (
  <Container>
    <Header
      content="Ship"
    />
  </Container>
);


export default Ship;
